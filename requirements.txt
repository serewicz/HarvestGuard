streamlit>=1.38.0,<1.51.0  # >=1.51 changed the local dev-server model (frontend no longer served on the same port as the backend under `streamlit run`) -- confirmed against a fresh install, not yet compatible with how this app is run/containerized
boto3>=1.35.0
google-cloud-storage>=2.18.0
azure-storage-blob>=12.19.0
azure-identity>=1.15.0
semgrep>=1.90.0  # code_analysis/ -- adds ~160MB to the container image, see docs/ROADMAP.md Pillar 1
cryptography>=41.0.0  # scanner/crypto_inventory.py imports x509/hazmat.primitives directly (pkcs12, dsa/ec/ed448/ed25519, hashes, serialization) -- was previously only a transitive dep of azure-identity/google-auth/msal, which is not something first-party code should rely on
certifi>=2024.2.2  # code_analysis/scanner.py calls certifi.where() directly for semgrep's CA bundle -- was previously only a transitive dep of httpx/requests
pandas>=2.2.0
plotly>=5.24.0
pydantic>=2.9.0
prometheus-client>=0.21.0
python-dotenv>=1.0.1
weasyprint>=62.0  # For PDF reports (may need system deps)
